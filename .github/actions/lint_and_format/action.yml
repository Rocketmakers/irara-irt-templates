name: Lint and format
description: Runs linting and formatting and ensuring everything is valid
runs:
  using: composite
  steps:
    - name: Setup environment
      uses: ./.github/actions/setup
    - name: Lint and format
      run: pnpm turbo lint --concurrency 2
      shell: bash
